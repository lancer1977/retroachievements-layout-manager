# Memory

Long-lived maintenance notes for retroachievements-layout-manager belong here.

Current validation note: `bash scripts/validate.sh` is the portable stewardship
gate and parses project, config, RESX, and XML files with `xmllint`.
